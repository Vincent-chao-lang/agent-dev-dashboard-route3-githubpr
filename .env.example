# Agent Dev Dashboard Environment Variables

# ========================================
# LLM Configuration - GLM-4.7
# ========================================
# Get your API key from: https://open.bigmodel.cn/
LLM_API_KEY=your-zhipuai-api-key-here
LLM_BASE_URL=https://open.bigmodel.cn/api/paas/v4/
LLM_MODEL=glm-4-plus
LLM_TEMPERATURE=0.7
LLM_MAX_TOKENS=4096

# ========================================
# Application Configuration
# ========================================
# Database URL
# For development (SQLite - default):
#   DATABASE_URL=sqlite:////path/to/dashboard.sqlite
#   Or just set APP_DB_PATH and omit DATABASE_URL
# For production (PostgreSQL):
#   DATABASE_URL=postgresql://user:password@host:5432/database
# Examples:
#   DATABASE_URL=postgresql://agent_dashboard:secret@localhost:5432/agent_dashboard
#   DATABASE_URL=postgresql://agent_dashboard:secret@postgres-service:5432/agent_dashboard
DATABASE_URL=sqlite:////app/dashboard.sqlite

# Legacy: Database path (only used if DATABASE_URL is not set)
APP_DB_PATH=./dashboard.sqlite

# Workspace directory (default: ./workspace)
WORKSPACE_DIR=./workspace

# Session secret for cookie encryption (CHANGE THIS IN PRODUCTION!)
SESSION_SECRET=dev-insecure-secret-change-me-in-production

# Maximum background workers (default: 4)
MAX_WORKERS=4

# Database connection pool size (for PostgreSQL only, default: 5)
DB_POOL_SIZE=5
# Database max overflow connections (for PostgreSQL only, default: 10)
DB_MAX_OVERFLOW=10

# ========================================
# GitHub Integration (Optional)
# ========================================
# GitHub personal access token for PR operations
GITHUB_TOKEN=

# ========================================
# Other LLM Providers (Optional)
# ========================================
# To use OpenAI instead of GLM:
# LLM_PROVIDER=openai
# LLM_API_KEY=your-openai-api-key
# LLM_BASE_URL=https://api.openai.com/v1
# LLM_MODEL=gpt-4
